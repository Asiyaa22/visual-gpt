# Dockerfile - For deploying evaluator with Playwright + Node.js on Render

# 1. Use the official Node.js image with Debian for Playwright
FROM mcr.microsoft.com/playwright:v1.44.0-jammy

# 2. Set working directory
WORKDIR /app

# 3. Copy package files and install dependencies
COPY package.json package-lock.json* ./
RUN npm install

# 4. Copy rest of your project
COPY . .

# 5. Expose port (same as your server.js uses)
EXPOSE 3000

# 6. Start the server
CMD ["node", "server.js"]
